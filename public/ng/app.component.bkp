var app = angular.module('integro', []);

app.controller('table-contents', function($scope, $http){

	console.log('scope');
	console.log($scope);
	console.log('http');
	console.log($http);

	var t = 'Testing variable';

	$scope.contents = [{
		name : t,
		discipline : t,
		date : t	
		}];
});