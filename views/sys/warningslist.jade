form
    input.form-control(type="search", placeholder="Pesquisa", name="searchwarnings", [value]="", title="", (keyup)="search()", [(ngModel)]="searchquery")
    i.text-muted X Resultados para {{ searchquery }}
dl
 - console.log(searchlist)
 .search-result(*ngFor="#item of searchlist") 
  dt {{ item.title }}
  dl {{ item.details }}

dl
for item, key in warnings
 dt= item.title
 dl= item.details
